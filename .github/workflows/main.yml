name: Simple CI Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run simple command
        run: |
          echo "Hello GitHub Actions!"
          echo "CI/CD workflow is running successfully"
